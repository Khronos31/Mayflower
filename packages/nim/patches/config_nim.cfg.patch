--- a/config/nim.cfg
+++ b/config/nim.cfg
@@ -377,3 +377,9 @@
   clang.options.linker %= "${clang.options.linker} -s"
   clang.cpp.options.linker %= "${clang.cpp.options.linker} -s"
 @end
+
+# Rootless jailbroken iOS: sign with ldid. posix_spawn/execve retry
+# /var/jb/bin/sh on EPERM in posix.nim; no libiosexec.
+@if ios:
+  ldid.entitlements = "/var/jb/usr/share/nim/entitlements.plist"
+@end
